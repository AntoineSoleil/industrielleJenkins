node {
    stage("Test") {
        // Run `composer update` as a shell script
       // sh 'composer install'
        echo 'test'
    }
    stage("phpunit") {
        // Run PHPUnit
      //  sh 'vendor/bin/phpunit'
    }
}
